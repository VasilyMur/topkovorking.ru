mixin paginationOkrug(page, pages, tagOriginal)
  .pagination
    .pagination__prev
      if page > 1
        a(href=`/okrug/${tagOriginal}/page/${page - 1}`) Назад
    .pagination__text
      p Страница #{page} из #{pages}
    .pagination__next
      if page < pages
        a(href=`/okrug/${tagOriginal}/page/${parseFloat(page) + 1}`) Вперед
